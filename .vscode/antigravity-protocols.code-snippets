{
	"Universal Core Protocol": {
		"prefix": "must-core",
		"body": [
			"",
			"---",
			"🚨 **CRITICAL EXECUTION PROTOCOL (MUST FOLLOW):**",
			"1. **Context Check:** İşleme başlamadan önce `memory/active_context.md` ve `memory/tech_stack.md` dosyalarını oku. Mevcut mimariye %100 uyumlu ol.",
			"2. **Strict TypeScript:** `any` kullanımı KESİNLİKLE yasaktır. Gerekirse yeni Interface/Type tanımla ve bunu `types/` klasörüne ekle.",
			"3. **No Placeholders:** Kodun içinde asla 'TODO', 'Buraya mantık gelecek' gibi eksik kısımlar bırakma. Üretilen kod çalıştırılabilir ve bitmiş olmalı.",
			"4. **Error Handling:** Olası hata durumlarını (try-catch, API failures) öngör ve kullanıcıya sessizce hata vermek yerine anlamlı geri bildirim mekanizmasını entegre et.",
			"5. **Memory Update:** İşlem bittikten sonra, eğer önemli bir değişiklik yaptıysan `memory/active_context.md` dosyasını güncellemeni bekliyorum.",
			"---"
		],
		"description": "Genel kodlama kuralları ve hafıza kontrolü"
	},
	"UI/UX Protocol": {
		"prefix": "must-ui",
		"body": [
			"",
			"---",
			"🎨 **UI/UX & MOBILE-FIRST PROTOCOL:**",
			"1. **Mobil Uyumluluk (HIG):** Tasarladığın her bileşen, iOS Safari'de kusursuz görünmeli. Dokunma hedefleri (Touch Targets) min 44px olmalı.",
			"2. **Tailwind Only:** Sadece Tailwind CSS class'larını kullan. Karmaşık animasyonlar için `framer-motion` kullan, inline style yasak.",
			"3. **Responsive Check:** Kodunu yazarken şu breakpointleri kontrol et: `sm` (Mobil), `md` (Tablet), `lg` (Desktop). Mobilde taşma (overflow) olmadığından emin ol.",
			"4. **Accessibilty (A11y):** Tüm interaktif elemanlarda `aria-label` ve klavye navigasyonu (tab-index) desteği olduğundan emin ol.",
			"5. **Glassmorphism 3.0:** Projemiz Light Mode Glassmorphism kullanır. Base styling: `bg-white/60 backdrop-blur-xl border border-white/40 shadow-lg`. Dark arka planlar YASAK.",
			"6. **Animation:** Framer Motion ile smooth animasyonlar ekle. `whileHover`, `whileTap`, `AnimatePresence` kullan.",
			"---"
		],
		"description": "Frontend, Tasarım ve Mobil uyumluluk kuralları"
	},
	"Data Protocol": {
		"prefix": "must-data",
		"body": [
			"",
			"---",
			"🛡️ **DATA INTEGRITY & SECURITY PROTOCOL:**",
			"1. **Source of Truth:** Veri şeması için `memory/database_schema.md` dosyasını referans al. API kontratları için `memory/api_contracts.md` dosyasını kontrol et. Kendi kafandan sütun uydurma.",
			"2. **Type Safety:** Supabase'den gelen veriyi frontend'de kullanmadan önce Zod veya TypeScript ile tip dönüşümünü (Type Casting) güvenli yap. Quest types için `src/types/database.types.ts` referans al.",
			"3. **Secure Fetching:** Tüm veritabanı işlemlerini Server Component veya Server Action içinde yap. Client-side'da hassas veri ifşa etme.",
			"4. **JSONB Handling:** `logs` tablosundaki JSON verisini işlerken null check yapmayı unutma.",
			"5. **Quest Engine:** XP ve Level hesaplamaları için `src/lib/questEngine.ts` kullan. Formülleri değiştirme.",
			"---"
		],
		"description": "Backend, Supabase ve Veri güvenliği kuralları"
	},
	"Auth Protocol": {
		"prefix": "must-auth",
		"body": [
			"",
			"---",
			"� **AUTHENTICATION PROTOCOL:**",
			"1. **Gerçek Kullanıcı Zorunlu:** Demo user modu kaldırıldı. Tüm server actions `getAuthenticatedClient()` kullanmalı.",
			"2. **Auth Utility:** `src/lib/auth.ts` dosyasındaki `getAuthenticatedClient()` ve `requireAuth()` fonksiyonlarını kullan.",
			"3. **Error Handling:** `AuthenticationError` yakalayarak kullanıcıyı `/login` sayfasına yönlendir.",
			"4. **Protected Routes:** `middleware.ts` dosyasında korunan route'ları kontrol et.",
			"5. **Session Refresh:** Supabase SSR ile session otomatik yenilenir, manuel müdahale gerekmiyor.",
			"---"
		],
		"description": "Authentication ve kullanıcı oturumu kuralları"
	},
	"Quest Protocol": {
		"prefix": "must-quest",
		"body": [
			"",
			"---",
			"⚡ **QUEST SYSTEM PROTOCOL:**",
			"1. **Template System:** Quest oluştururken `quest_templates` tablosundaki 124 şablonu kullan. `getQuestTemplates(category)` ile şablonları getir.",
			"2. **XP Calculation:** XP hesaplamaları için `src/lib/questEngine.ts` kullan. Formül: `baseXP + difficultyBonus + streakBonus + timeBonus`.",
			"3. **Streak Logic:** Streak hesaplamaları için `src/lib/streakEngine.ts` kullan. Streak kırılma mantığını değiştirme.",
			"4. **Goal Integration:** Questler opsiyonel olarak `goal_id` ile hedefe bağlanabilir. Bu bağlantıyı UI'da göster.",
			"5. **Completion Flow:** Quest tamamlandığında `completeQuest()` action'ını çağır. XP breakdown ve animations'ı handle et.",
			"---"
		],
		"description": "Quest System ve gamification kuralları"
	}
}